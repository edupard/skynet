apiVersion: batch/v1
kind: Job
metadata:
  name: transpose
  labels:
    jobgroup: transpose
spec:
  completions: 1
  backoffLimit: 1
  template:
    metadata:
      name: transpose
      labels:
        jobgroup: transpose
    spec:
      volumes:
        - name: secret-volume
          secret:
            secretName: hinton-svc-key
      containers:
        - name: worker
          image: gcr.io/skynet-1984/skynet:c64437c465b3e79414603cf96b51aa2bbe393026
          command: ["python"]
          args: ["job_transpose.py", "24"]
          volumeMounts:
            - name: secret-volume
              readOnly: true
              mountPath: "/etc/secret-volume"
          ports:
            - containerPort: 5000
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /etc/secret-volume/key
            - name: TIINGO_API_KEY
              valueFrom:
                secretKeyRef:
                  name: tiingo-credentials
                  key: token
      restartPolicy: Never