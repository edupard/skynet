apiVersion: apps/v1
kind: Deployment
metadata:
  name: _SERVICE
  labels:
    app: _SERVICE
spec:
  replicas: _REPLICAS
  selector:
    matchLabels:
      app: _SERVICE
  template:
    metadata:
      labels:
        app: _SERVICE
    spec:
      volumes:
        - name: secret-volume
          secret:
            secretName: hinton-svc-key
      containers:
        - name: worker
          image: gcr.io/skynet-1984/_SERVICE:_COMMIT_SHA
          volumeMounts:
            - name: secret-volume
              readOnly: true
              mountPath: "/etc/secret-volume"
          ports:
            - containerPort: 5000
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /etc/secret-volume/key
            - name: TIINGO_API_KEY
              valueFrom:
                secretKeyRef:
                  name: tiingo-credentials
                  key: token
